
import { PageTitle } from "@/components/page-title";

export default function AppSettingsPage() {
  return (
    <div>
      <PageTitle title="Application Settings" description="Manage feature flags, company profile, and global settings." />
      <div className="mt-8">
        <p className="text-center text-muted-foreground">Application settings interface coming soon.</p>
      </div>
    </div>
  );
}
